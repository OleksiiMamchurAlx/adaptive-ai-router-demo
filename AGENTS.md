# Portfolio evidence demo

Preserve the offline, standard-library-only demo. Do not enable external model calls or copy runtime databases into this repository. Run the tests after changes. Keep historical evidence, new tests and synthetic fixtures distinct. Preserve Limitations and AI disclosure. Public changes must pass the publication allowlist, secret/path scans, link checks and claim review before push. Do not infer new publication authority from this file; use the owner's current request.
